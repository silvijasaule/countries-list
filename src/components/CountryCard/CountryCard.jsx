import React from "react";

export const CountryCard = ({ data }) => {
    const countryData = data;
    const countryName = countryData?.name;
    const region = countryData?.region;
    const size = countryData?.size;

    return(
        <div></div>
    )
  
}